import PlaceholderPage from '../components/common/PlaceholderPage'

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Configure Study Buddy — LLM provider, API credentials, notification preferences, and accessibility options."
      session="a future session"
    />
  )
}
