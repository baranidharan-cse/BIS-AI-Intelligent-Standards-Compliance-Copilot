import PlaceholderPage from '../components/common/PlaceholderPage'

export default function ProfilePage() {
  return (
    <PlaceholderPage
      title="Profile"
      description="Your learner profile — overall progress, mastery breakdown by subject, total study time, and achievement badges."
      session="a future session"
    />
  )
}
