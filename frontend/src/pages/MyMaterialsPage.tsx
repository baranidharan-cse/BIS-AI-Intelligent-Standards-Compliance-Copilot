import PlaceholderPage from '../components/common/PlaceholderPage'

export default function MyMaterialsPage() {
  return (
    <PlaceholderPage
      title="My Materials"
      description="Upload and manage your study documents — PDFs, text files, and web URLs. The ingestion pipeline will extract structure, sections, and concepts automatically."
      session="Session 2 (Ingestion)"
    />
  )
}
