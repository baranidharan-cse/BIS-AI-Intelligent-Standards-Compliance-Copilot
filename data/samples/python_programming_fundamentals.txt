Python Programming Fundamentals
===============================

A structured introduction to Python for beginners. This document covers the
seven core topics every Python programmer must master before moving to
intermediate concepts.

---

1. VARIABLES
============

A variable is a named reference to a value stored in memory. In Python you
do not declare a type — you simply assign a value and Python infers the type.

    # Assigning values to variables
    name = "Alice"
    age = 30
    height = 5.6
    is_student = True

Rules for variable names:
  - Must start with a letter or underscore (_)
  - Can contain letters, digits, and underscores
  - Case-sensitive: `score` and `Score` are different variables
  - Cannot be a Python keyword (if, for, while, def, class, etc.)

Variables can be reassigned at any time:

    x = 10
    x = x + 5   # x is now 15
    x = "hello" # x is now a string — Python allows this (dynamic typing)

---

2. DATA TYPES
=============

Python has several built-in data types. The most common are:

  int     — whole numbers: 0, -5, 42, 1_000_000
  float   — decimal numbers: 3.14, -0.5, 2.0e8
  str     — text in single or double quotes: 'hello', "world"
  bool    — True or False
  NoneType — the special value None (represents "no value")

Checking a type:

    print(type(42))        # <class 'int'>
    print(type("hello"))   # <class 'str'>
    print(type(3.14))      # <class 'float'>
    print(type(True))      # <class 'bool'>
    print(type(None))      # <class 'NoneType'>

Type conversion (casting):

    int("42")      # 42
    float(7)       # 7.0
    str(100)       # "100"
    bool(0)        # False (0, "", [], None are all falsy)
    bool(1)        # True

---

3. CONDITIONS
=============

Conditions let your program make decisions. Python uses if, elif, and else.

Basic syntax:

    if condition:
        # runs when condition is True
    elif other_condition:
        # runs when other_condition is True
    else:
        # runs when none of the above are True

Example:

    score = 85

    if score >= 90:
        grade = "A"
    elif score >= 80:
        grade = "B"
    elif score >= 70:
        grade = "C"
    else:
        grade = "F"

    print(f"Your grade is {grade}")   # Your grade is B

Comparison operators:
  ==  equal to
  !=  not equal to
  <   less than
  >   greater than
  <=  less than or equal to
  >=  greater than or equal to

Logical operators:
  and  — both conditions must be True
  or   — at least one condition must be True
  not  — inverts a boolean

    if age >= 18 and has_id:
        print("Access granted")

---

4. LOOPS
========

Loops repeat a block of code multiple times.

4.1 for loop
------------
Use a for loop to iterate over a sequence (list, string, range, etc.).

    for i in range(5):
        print(i)   # prints 0, 1, 2, 3, 4

    fruits = ["apple", "banana", "cherry"]
    for fruit in fruits:
        print(fruit)

    # range(start, stop, step)
    for i in range(0, 10, 2):
        print(i)   # 0, 2, 4, 6, 8

4.2 while loop
--------------
Use a while loop when you don't know in advance how many iterations you need.

    count = 0
    while count < 5:
        print(count)
        count += 1

Loop control:
  break    — exit the loop immediately
  continue — skip to the next iteration
  pass     — do nothing (placeholder)

    for i in range(10):
        if i == 5:
            break       # stops at 5
        if i % 2 == 0:
            continue    # skips even numbers
        print(i)        # prints 1, 3

---

5. FUNCTIONS
============

A function is a reusable block of code that performs a specific task.

Defining a function:

    def function_name(parameter1, parameter2):
        # body
        return result

Example:

    def greet(name):
        return f"Hello, {name}!"

    message = greet("Alice")
    print(message)   # Hello, Alice!

Parameters and arguments:
  - Parameters are placeholders defined in the function signature.
  - Arguments are the actual values passed when calling the function.

Default parameter values:

    def power(base, exponent=2):
        return base ** exponent

    print(power(3))     # 9  (exponent defaults to 2)
    print(power(3, 3))  # 27

Multiple return values:

    def min_max(numbers):
        return min(numbers), max(numbers)

    low, high = min_max([3, 1, 4, 1, 5, 9])
    print(low, high)   # 1 9

Docstrings (recommended):

    def add(a, b):
        """Return the sum of a and b."""
        return a + b

---

6. LISTS
========

A list is an ordered, mutable collection of items.

Creating lists:

    empty = []
    numbers = [1, 2, 3, 4, 5]
    mixed = [1, "hello", True, 3.14]
    nested = [[1, 2], [3, 4], [5, 6]]

Accessing items (zero-indexed):

    fruits = ["apple", "banana", "cherry"]
    print(fruits[0])    # apple
    print(fruits[-1])   # cherry (last item)
    print(fruits[1:3])  # ['banana', 'cherry'] (slicing)

Modifying lists:

    fruits.append("date")         # add to end
    fruits.insert(1, "avocado")   # insert at index 1
    fruits.remove("banana")       # remove by value
    popped = fruits.pop()         # remove and return last item
    fruits[0] = "apricot"         # replace by index

Useful list methods:

    numbers = [3, 1, 4, 1, 5, 9, 2, 6]
    numbers.sort()          # sorts in place
    numbers.reverse()       # reverses in place
    length = len(numbers)   # number of items
    total = sum(numbers)    # sum of all items
    count = numbers.count(1) # occurrences of 1

List comprehensions (concise way to build lists):

    squares = [x ** 2 for x in range(10)]
    evens = [x for x in range(20) if x % 2 == 0]

---

7. DICTIONARIES
===============

A dictionary is an unordered collection of key-value pairs.
Keys must be unique and immutable (strings, numbers, tuples).

Creating dictionaries:

    empty = {}
    person = {
        "name": "Alice",
        "age": 30,
        "city": "London"
    }

Accessing values:

    print(person["name"])           # Alice
    print(person.get("age"))        # 30
    print(person.get("email", "N/A"))  # N/A (default if key missing)

Adding and modifying:

    person["email"] = "alice@example.com"  # add new key
    person["age"] = 31                     # modify existing key

Removing:

    del person["city"]          # remove by key
    age = person.pop("age")    # remove and return value

Iterating over a dictionary:

    for key in person:
        print(key, person[key])

    for key, value in person.items():
        print(f"{key}: {value}")

    keys = list(person.keys())
    values = list(person.values())

Checking membership:

    if "name" in person:
        print("Name exists!")

Nested dictionaries:

    students = {
        "alice": {"grade": "A", "score": 95},
        "bob":   {"grade": "B", "score": 83},
    }
    print(students["alice"]["score"])   # 95

---

PRACTICE EXERCISES
==================

1. Write a function that takes a list of numbers and returns only the even ones.
2. Create a dictionary of 5 countries and their capitals. Print each pair.
3. Write a while loop that prints the Fibonacci sequence up to 100.
4. Write a function that checks if a word is a palindrome.
5. Using a list comprehension, create a list of the first 10 perfect squares.

---

NEXT STEPS
==========

After mastering these fundamentals, explore:
  - Object-Oriented Programming (classes and objects)
  - File I/O (reading and writing files)
  - Error handling (try / except)
  - Modules and packages (import)
  - Standard library highlights: os, sys, math, datetime, json
